<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%=title%></title>
  </head>
  <body>
    <h1><%=title%></h1>
    <ul>
      <% instruments.forEach(item=>{ %>
      <li >
        <p><%=item.instrument%></p>
        <p>Quantity: <%=item.quantity%></p>
        
        <p>Price: $<%=item.price%></p>
        <a href="/product/<%=item.id%>">View Instrument</a>
      </li>

      <%})%>
    </ul>
    <a href="/">Homepage</a>
    <a href="/product/add">Add Item</a>
  </body>
</html>
